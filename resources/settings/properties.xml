<properties>
    <!-- HR Cap Setting -->
    <!-- TODO: Wire this to the Connect IQ app settings -->
    <property id="hrCapBpm" type="number">160</property>
    
    <!-- Goal Mode Setting -->
    <!-- TODO: Wire this to the Connect IQ app settings -->
    <!-- 0 = CLASS only, 1 = Hybrid (default), 2 = COMBAT only -->
    <property id="goalMode" type="number">1</property>
    
    <!-- Z1 Target Minutes Setting -->
    <property id="z1TargetMin" type="number">180</property>
</properties>